import { ManageOrders } from '@/modules/dashboard/Admin/ManageOrders';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <ManageOrders />
    </>
  );
};

export default page;
