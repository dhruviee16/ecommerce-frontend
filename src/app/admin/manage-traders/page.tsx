import { ManageTraders } from '@/modules/dashboard/Admin/ManageTraders';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <ManageTraders />
    </>
  );
};

export default page;
