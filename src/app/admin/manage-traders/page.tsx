import { ManageTraders } from '@/modules/Admin/ManageTraders';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <ManageTraders />
    </>
  );
};

export default page;
