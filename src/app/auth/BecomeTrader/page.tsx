import { Layout } from '@/components/Layout';
import { TraderSignUpForm } from '@/modules/auth';

const page = () => {
  return (
    <Layout>
      <TraderSignUpForm />
    </Layout>
  );
};

export default page;
