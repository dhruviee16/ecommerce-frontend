import { Layout } from '@/components/Layout';
import { SignUpForm } from '@/modules/auth';

const page = () => {
  return (
    <Layout>
      <SignUpForm />
    </Layout>
  );
};

export default page;
