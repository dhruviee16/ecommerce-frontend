import { Layout } from '@/components/Layout';
import { TraderProfile } from '@/modules/dashboard/trader';

const page = () => {
  return (
    <Layout>
      <TraderProfile />
    </Layout>
  );
};

export default page;
